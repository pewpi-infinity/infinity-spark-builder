# UI Layout Rules (MANDATORY)

To prevent overflow and occlusion:

- Top buttons MUST wrap (flex-wrap)
- No horizontal overflow allowed
- Cockpit display MUST have z-index priority
- Menus may not cover primary displays

Violations are UI defects.
